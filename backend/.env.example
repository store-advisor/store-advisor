# Copy this file to backend/.env for local (non-Docker) development.
# backend/.env is gitignored — never commit real values here.

DATABASE_URL="postgresql://storeadvisor:localdev@localhost:5432/storeadvisor"
# The Python AI service (stage 3). Leave unset to run without explanations:
# checks and the API work fine, findings simply have no prose.
AI_SERVICE_URL="http://localhost:8000"
